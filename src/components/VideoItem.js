import React from 'react';

import {Grid, Paper, Typography} from '@material-ui/core';

// No state so const
const VideoItem = () => {
    return (
        <h1>Video Item</h1>
    )
}

export default VideoItem;